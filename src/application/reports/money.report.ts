/** Денежное значение в стабильных report contracts. */
export interface ReportMoney {
  /** Валюта денежного значения. */
  currency: string;
  /** Сумма в строковом формате decimal. */
  amount: string;
}
